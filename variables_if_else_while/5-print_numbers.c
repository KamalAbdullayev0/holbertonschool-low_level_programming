#include <stdio.h>
/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	char  ulvi = '0';

	while (ulvi < 10)
	{
		putchar(ulvi);
		ulvi++;
	}

	putchar('\n');
	return (0);


}
